APP_ENV=dev
APP_SECRET=change-me-in-production

# Docker database URL (host = db service)
DATABASE_URL=postgresql://foliode:foliode@db:5432/foliode?serverVersion=13&charset=utf8

# Mail / workers
MAILER_DSN=smtp://mailhog:1025
MESSENGER_TRANSPORT_DSN=doctrine://default?auto_setup=0

# CORS
CORS_ALLOW_ORIGIN=^https?://(localhost|127\\.0\\.1)(:[0-9]+)?$

# Debug
VAR_DUMPER_SERVER=127.0.0.1:9912

# JWT
JWT_PASSPHRASE=change-this-passphrase
JWT_SECRET_KEY=%kernel.project_dir%/config/jwt/private.pem
JWT_PUBLIC_KEY=%kernel.project_dir%/config/jwt/public.pem
